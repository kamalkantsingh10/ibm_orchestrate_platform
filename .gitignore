# Claude Code local settings (user-specific, should not be committed)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Playwright MCP session logs (per-developer; auto-generated)
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.env
.env.*
!.env.example

# Node
node_modules/
dist/
build/
.next/
.turbo/
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Coverage / test
.coverage
coverage/
htmlcov/
.nyc_output/

# Logs (Story 2.x dev-runtime output lands in logs/; folder kept via .gitkeep)
*.log
logs/*
!logs/.gitkeep

# SQLite demo DB (Story 1.5). Folder kept via data/.gitkeep; the .db file
# is per-developer mutable state and must not be committed.
data/cockpit.db
data/cockpit.db-journal
data/cockpit.db-wal
data/cockpit.db-shm

# Story 3.1 — append-only JSONL ledger. Per-developer mutable state.
data/ledger.jsonl
apps/agents/data/ledger.jsonl
apps/agents/data/cockpit.db
apps/agents/data/cockpit.db-journal
apps/agents/data/cockpit.db-wal
apps/agents/data/cockpit.db-shm
apps/cockpit-api/data/ledger.jsonl
apps/cockpit-api/data/cockpit.db
apps/cockpit-api/data/cockpit.db-journal
apps/cockpit-api/data/cockpit.db-wal
apps/cockpit-api/data/cockpit.db-shm

# Story 3.8 — uploaded PDFs and generated sample PDFs are per-developer
# mutable state, not committed. demo-reset wipes ./fixtures/uploads/.
fixtures/uploads/
fixtures/sample_pdfs/

# TanStack Router file-based codegen output (re-enable swap; cockpit-ui).
apps/cockpit-ui/src/routeTree.gen.ts
